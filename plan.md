#视觉模型运行框架实现计划

## 1. 功能需求描述
开始接触视觉模型运行部署后，就需要考虑如下问题：
+ 一个清单文件管理多个模型；
+ 不同平台如何适配扩展(统一的输入输出)；
+ 可扩展实现的运行时后端实现（高通、英伟达、RK等）；

参考目前开源方案，设计一套简化的运行时框架，用于通过一个清单文件管理多个模型及其运行配置，
1. 程序初始解析清单文件，从清单文件中获取到版本，名称，模型列表，各个模型的运行backend、输入输出信息等；
2. 接下来初始化准备好环境，上层应用可以通过统一的接口和配置简化对模型使用；

## 2. AI设计方案
利用AI模型针对上述需求描述进行分析评估，整理对比出合适或者可以参考的方案，并提供架构设计图。开发再通过几轮与AI的沟通形成最终方案。注意，这个阶段不涉及具体的代码实现。

## 3. 开发计划
1. 搭建基于bazel 6.5的构建环境；
2. 设计实现基础模块：
   + 解析清单文件；
   + 初始化运行环境；
   + 提供统一的接口和配置；
3. 开发测试；
4. 发布。